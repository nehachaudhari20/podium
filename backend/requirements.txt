# Core dependencies
pyyaml>=6.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Phase 3+ LLM (optional: pip install 'podium-backend[gemini]' or pip install google-genai)
google-genai>=1.0.0

# Dev (also declared in pyproject.toml [dev])
pytest>=8.0
httpx>=0.27.0
