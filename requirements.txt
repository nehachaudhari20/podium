# Core dependencies (expand per phase)
pyyaml>=6.0

# Phase 3+
# anthropic>=0.40.0

# Phase 8 dashboard
# fastapi>=0.115.0
# uvicorn[standard]>=0.32.0

# Dev (also declared in pyproject.toml [dev])
pytest>=8.0
